import { render,screen } from "@testing-library/react";import { describe,expect,it } from "vitest";import { BenchmarkPanel } from "./BenchmarkPanel";
describe("BenchmarkPanel",()=>{it("masque les valeurs quand la cohorte est insuffisante",()=>{render(<BenchmarkPanel data={{available:false,cohortSize:4,minimumCohortSize:10,userNcRate:null,cohortMedianNcRate:null,percentile:null,message:"Cohorte insuffisante"}}/>);expect(screen.getByText(/4\/10 organisations/)).toBeInTheDocument();expect(screen.queryByText(/Votre taux de NC/)).not.toBeInTheDocument();});it("affiche uniquement les agrégats autorisés",()=>{render(<BenchmarkPanel data={{available:true,cohortSize:12,minimumCohortSize:10,userNcRate:18,cohortMedianNcRate:24,percentile:35,message:"Comparaison anonymisée"}}/>);expect(screen.getByText("18%")).toBeInTheDocument();expect(screen.getByText("24%")).toBeInTheDocument();expect(screen.getByText("P35")).toBeInTheDocument();});});
