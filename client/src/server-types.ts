/**
 * Standalone frontend type shim.
 *
 * If you keep frontend & backend in a single monorepo, replace this file by:
 *   export type { AppRouter } from "<path-to-backend>/server/routers";
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type AppRouter = any;
