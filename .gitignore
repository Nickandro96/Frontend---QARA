node_modules/
.pnpm/
dist
.env
.env.*
test-results/
playwright-report/
# pnpm-lock.yaml is the single source of truth for this repo (see
# docs/audit/02-audit-technique.md, M-02) — don't let npm regenerate this.
package-lock.json
